services:
  qiaopi:
    build: .
    container_name: qiaopi
    restart: unless-stopped
    env_file:
      - .env
    environment:
      HOST: 0.0.0.0
      PORT: 8000
    ports:
      - "${PORT:-8000}:8000"
    volumes:
      - ./data:/app/data
      - ./static/music:/app/static/music
